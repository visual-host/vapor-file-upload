{
    "name": "visual-host/vapor-file-upload",
    "description": "A Laravel Nova field.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "VisualHost\\VaporFileUpload\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "VisualHost\\VaporFileUpload\\FieldServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
